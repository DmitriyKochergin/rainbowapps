import { FC } from 'react';

import './Crocodile.scss';

const Crocodile: FC = (): JSX.Element => {

  return (
    <div className={'p-20'}>
      Crocodile
    </div>
  );
};

export default Crocodile;
